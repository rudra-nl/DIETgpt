{
  "name": "dietgpt",
  "version": "1.0.0",
  "private": true,
  "description": "DietGPT frontend",
  "type": "commonjs",

  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html --dist-dir dist",
    "start": "npm run dev"
  },

  "dependencies": {
    "@google/genai": "^1.27.0"
  },

  "devDependencies": {
    "parcel": "^2.16.0",
    "buffer": "^6.0.3",
    "process": "^0.11.10"
  }
}
